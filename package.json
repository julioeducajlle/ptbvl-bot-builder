{
  "private": true,
  "scripts": {
    "build": "npx esbuild app.tsx --bundle --platform=browser --outfile=bundle.js --external:tasklet-bridge --loader:.ts=ts --loader:.tsx=tsx",
    "dev": "npx esbuild app.tsx --bundle --platform=browser --outfile=bundle.js --watch --external:tasklet-bridge --loader:.ts=ts --loader:.tsx=tsx",
    "generate-index": "python3 scripts/generate_index.py"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "lucide-react": "^0.523.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "esbuild": "^0.21.0"
  }
}